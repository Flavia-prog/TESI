# Archived Legacy Noise Configs

Created on 2026-05-16 during repository cleanup.

These fixed-noise DP configs are kept for historical reproducibility but are no
longer the active thesis matrix. Current baseline configs live in
`configs/current/baselines/`; current sigma-based DP matrix configs live in
`configs/current/dp_sigma_matrix/`.
